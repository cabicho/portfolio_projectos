# 🐳 Docker Compose - Mozambique Health Pipeline

## Serviços Disponíveis

### 🚀 Início Rápido
```bash
# Iniciar todos os serviços
./docker-utils.sh start

# Ou usar docker-compose diretamente
docker-compose up -d
